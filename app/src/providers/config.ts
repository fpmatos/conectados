var apiServer = "http://conectadosapi.dev.intranet.duratex";
var cmsServer = "http://conectadoscms.dev.intranet.duratex";

// var apiServer = "http://localhost:58957";
// var cmsServer = "http://localhost:4200";

export let AUTENTICACAO_URL: string = `${apiServer}/token`;
export let ROOT_API_URL: string = `${apiServer}/api/`;
export let CMS_URL: string = cmsServer;
